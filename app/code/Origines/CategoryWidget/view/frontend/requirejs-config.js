var config = {
    
    // When load 'requirejs' always load the following files also
    map: {
        '*': {
            slider: 'Origines_CategoryWidget/js/slider'            
        }
    }
};